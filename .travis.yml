language: javascript

services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

cache:
  directories:
    - node_modules

script:
  - docker-compose build 
  - docker-compose run frontend npm run test:ci

