services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

cache:
  directories:
    - node_modules

script:
  - docker-compose run frontend npm run build
  - docker-compose run frontend npm run test:ci

